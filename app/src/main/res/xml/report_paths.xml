<?xml version="1.0" encoding="utf-8"?>
<!-- Only the reports folder, so the share sheet can reach the report and nothing else in the cache. -->
<paths>
    <cache-path name="reports" path="reports/" />
</paths>
